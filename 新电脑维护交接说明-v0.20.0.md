# 《星港拾荒者》新电脑维护交接说明

> 当前版本：v0.20.0「星雨寄航」  
> 整理日期：2026-08-07  
> 目的：换电脑后仅凭本文件和 GitHub 仓库即可继续开发、发布、管理 Firebase 与排查玩家问题。

## 1. 项目地址

- GitHub 仓库：<https://github.com/lv5accelerator-xy/my-html-game>
- 在线游戏：<https://lv5accelerator-xy.github.io/my-html-game/>
- 默认分支：`main`
- Firebase 项目 ID：`stellar-outpost-idle`
- Firebase 控制台：<https://console.firebase.google.com/project/stellar-outpost-idle/overview>

项目是纯 HTML/CSS/JavaScript 游戏，不使用 React、打包器或后端服务器。GitHub Pages 负责网页发布；Firebase Authentication 与 Firestore 负责 Google 登录、云存档、排行榜、公告和反馈。

## 2. 新电脑第一次接手

1. 安装 Git，并登录拥有仓库权限的 GitHub 账号。
2. 克隆仓库：

   ```bash
   git clone https://github.com/lv5accelerator-xy/my-html-game.git
   cd my-html-game
   ```

3. 本地预览可运行：

   ```bash
   python -m http.server 8000
   ```

   然后访问 <http://localhost:8000/>。不要只双击 HTML 测试云端模块，因为浏览器可能限制 `file://` 下的模块与登录弹窗。

4. 在 Firebase CLI 中登录并选择项目：

   ```bash
   npm install -g firebase-tools
   firebase login
   firebase use stellar-outpost-idle
   ```

5. 若使用 Codex 继续维护，把仓库文件夹作为工作区，然后告诉 Codex：“先完整阅读 `新电脑维护交接说明-v0.20.0.md`、`README.md`、`COMMUNICATIONS-GUIDE.txt` 和最新 Patch Notes，再继续开发。”

任何 GitHub、Google 或 Firebase 密码都不应写入仓库。本项目中的 Firebase Web 配置是网页客户端公开配置，真正的数据权限由 `firestore.rules` 控制。

## 3. 当前版本的关键内容

v0.20.0 新增两周限时活动「星雨寄航」：

- 开启：2026-08-08 00:00 UTC，即多伦多时间 2026-08-07 20:00。
- 观测结束：2026-08-23 00:00 UTC；界面文案称活动持续至 8 月 22 日。
- 兑换关闭：2026-09-23 00:00 UTC；界面文案称兑换开放至 9 月 22 日。
- 每日三选一星路，最近三天可以追赶。
- 可通过星尘获取、在线值守、手动回收、航站作业、主动战斗和远征航段完成。
- 单一活动货币「星雨余辉」，每日星路 240 枚，活动期间首次完成每封信 100 枚。
- 七封信笺每两日解锁；选择永久保存在本地与云端。
- 七档里程碑、六类兑换、七项纪念收藏，全部外观与收藏不增加永久倍率。
- 排行榜新增累计星雨余辉、完成星路和星雨信笺。
- 活动重要公告内置在客户端，即使 Firestore 公告读取失败也能出现。

活动时间常量位于 `game.js` 顶部：

```js
STARFALL_EVENT_START
STARFALL_EVENT_END
STARFALL_EXCHANGE_END
```

若以后延长活动，必须同时修改这些常量、游戏内公告、Patch Notes 和测试断言。

## 4. 核心文件说明

| 文件 | 用途 |
| --- | --- |
| `index.html` | 游戏入口、所有页面和弹窗结构、版本缓存参数 |
| `styles.css` | 桌面/手机视觉、星港、远征、作业、活动样式 |
| `game.js` | 游戏状态、数值、存档迁移、活动、任务、战斗、渲染与操作 |
| `game-math.js` | 独立数值函数，供游戏和数值测试共同使用 |
| `cloud-save.js` | Firebase 登录、云存档、排行榜、公告、反馈 |
| `firebase-config.js` | Firebase Web 客户端配置 |
| `firestore.rules` | 云存档、排行榜、公告与反馈的安全规则 |
| `COMMUNICATIONS-GUIDE.txt` | Firebase 公告发布和玩家反馈处理说明 |
| `FIREBASE-SETUP.txt` | Firebase 首次配置与故障排查 |
| `tests/` | 数值、桌面浏览器、手机与性能回归测试 |
| `历史版本/` | 每个正式版本的独立 ZIP 快照 |
| `星港拾荒者-v0.1.0-v0.20.0-Patch-Notes.txt` | 从最初版本至当前版本的累计更新记录 |

## 5. 存档与云端结构

- 浏览器主存档键：`stellarOutpostIdleSave_v1`。
- 当前本地存档结构版本：`SAVE_VERSION = 13`。
- 云端存档集合：`saves/{uid}`。
- 存档正文在 Firestore 中使用 JSON 字符串保存，避免“数组嵌套数组”被 Firestore 拒绝。
- 排行榜集合：`leaderboards/{uid}`。
- 公告集合：`announcements/{documentId}`。
- 反馈集合：`feedback/{autoId}`。

新增存档字段时：

1. 在 `freshState()` 或对应 `fresh...State()` 中提供默认值。
2. 在 `sanitizeState()` 或对应清洗函数中严格校验旧存档。
3. 结构发生变化时递增 `SAVE_VERSION`。
4. 确认云端 JSON 正文仍小于 700,000 字节。
5. 用旧版本存档执行桌面和手机测试，禁止要求玩家重开。

## 6. 公告与反馈管理

动态公告在 Firebase 控制台的 Firestore 数据库中编辑：

```text
announcements/{任意文档 ID}
```

建议字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `title` | string | 公告标题 |
| `body` | string | 公告正文 |
| `priority` | string | `normal` 或 `important` |
| `published` | boolean | `true` 才会对玩家显示 |
| `publishedAt` | timestamp | 发布时间 |
| `expiresAt` | timestamp，可选 | 到期后自动隐藏 |

v0.20.0 的活动公告同时内置在 `cloud-save.js` 的 `BUILTIN_ANNOUNCEMENTS` 中，文档 ID 为 `v0200-starfall-launch`。Firestore 中如使用相同 ID，客户端会去重。

玩家反馈在 `feedback` 集合查看。建议把 `status` 从 `new` 改为 `reviewing` 或 `resolved`；客户端无权读取其他玩家反馈。

完整步骤见 `COMMUNICATIONS-GUIDE.txt`。

## 7. Firestore 规则发布

v0.20.0 的排行榜新增三个字段，因此必须在网页发布前或同时部署最新版规则：

```bash
firebase deploy --only firestore:rules --project stellar-outpost-idle
```

发布后检查：

- 玩家只能读写自己的 `saves/{uid}`。
- 排行榜活动记录只能上升，不能回退。
- `starfallTotalEarned` 最大 99999。
- `starfallRoutes` 最大 15。
- `starfallLetters` 最大 7。
- 客户端只能读取 `published == true` 的公告。
- 登录玩家只能创建自己的反馈，不能读取反馈集合。

若云存档或榜单突然显示“权限不足”，首先确认控制台已部署仓库中的最新 `firestore.rules`，不要删除玩家存档。

## 8. 每次发布版本的固定流程

1. `git pull --ff-only`，确认没有未合并的其他设备修改。
2. 修改代码并更新 `GAME_VERSION`、`PATCH_NOTES_VERSION`；存档结构改变时再改 `SAVE_VERSION`。
3. 更新 `index.html` 的 meta 版本、页脚版本和四个资源 `?v=` 缓存参数。
4. 更新游戏内 `PATCH_NOTES`、累计 Patch Notes TXT 与 `README.md`。
5. 运行全部测试，并在桌面和 390px 手机宽度检查无横向滚动。
6. 若 Firestore 字段改变，先发布 `firestore.rules`。
7. 在 `历史版本/` 生成 `星港拾荒者-v版本号.zip`，并更新历史版本 README。
8. 提交并推送 `main`：

   ```bash
   git add -A
   git commit -m "feat: release v版本号"
   git push origin main
   ```

9. 等待 GitHub Actions / Pages 完成，再打开在线地址验证页脚、玩法和浏览器控制台。
10. 新版本发布后不要删除旧 ZIP；下一次更新也必须先确认上一版本已归档。

小修复使用 `0.x.y`，较大内容使用 `0.x.0`；`1.0.0` 保留给正式版。

## 9. 测试方法

核心检查：

```bash
node tests/numeric-balance.test.js
node tests/browser-smoke.test.js
node tests/mobile-performance.test.js
```

浏览器测试依赖 Playwright 和本机 Chrome。若提示缺少浏览器，可设置 `CODEX_CHROMIUM_PATH` 指向 Chrome，或执行 Playwright 的浏览器安装命令。

v0.20.0 必须重点检查：

- 旧存档能自动升级至 v13。
- 活动预告、进行、兑换、归档阶段正确。
- 8 月 10 日登录时生成 8、9、10 日三条可追赶星路。
- 星路固定三选一且确认后不能重选。
- 活动结束后不再获得余辉，兑换期仍可使用余辉。
- 七封信与七档里程碑均能渲染。
- 活动三个排行榜字段上传后不会被安全规则拒绝。
- 390px 手机页面宽度不超过视口。

## 10. 已确定的产品原则

- 不继续用巨大永久倍率制造后期内容；新增奖励优先做收藏、外观、临时策略和已有资源消耗。
- 新系统必须提供简短新手说明，界面优先折叠和渐进解锁，避免一次显示过多信息。
- 自动化购买不能让其他生产建筑的产量下降；同类增益的加法/乘法必须在 UI 中说清楚。
- 战斗与轮回奖励要控制通胀，并持续增加星尘、材料、组件、补给等资源回收项。
- 每次发布后游戏登录页显示由新到旧的 Patch Notes；设置菜单可随时查看。
- 每次发布都归档完整 ZIP，并保留累计 Patch Notes。
- 手机端禁止双击放大、页面左右滑动和因卡片最小宽度产生横向滚动。
- 玩家本地存档永远优先保证安全；云端失败不得清空或覆盖本地记录。

## 11. 下一步可考虑的内容

- 把限时活动框架整理为可复用模板，后续活动只替换数据、文案与外观。
- 增加星港配装方案的快速切换，但不要再增加第四套永久货币。
- 继续制作机制型首领与伴星收藏事件，奖励以图鉴和消耗材料为主。
- 增加全服合作目标时，必须使用服务器可验证的增量数据，避免客户端直接伪造总进度。
- 为 Firebase 公告增加开发者专用管理页前，应先设计管理员身份与服务端权限，不能把写权限开放给普通网页客户端。

## 12. 紧急恢复

- 网页出现严重问题：在 GitHub 上找到上一个稳定提交，优先修复后重新推送；不要对玩家要求清档。
- 云同步失败：先保留本地存档，让玩家使用“导出存档”；检查浏览器错误码和 Firestore 规则。
- 新版本缓存未刷新：确认 `index.html` 中所有资源 `?v=` 已更新，并检查 GitHub Pages 是否部署到最新提交。
- 数据异常：从浏览器三份轮换备份或玩家导出的 JSON 恢复；不要直接删除 Firebase 用户。
- 需要回看任意旧版本：从 `历史版本/星港拾荒者-v版本号.zip` 解压运行。

只要仓库、GitHub 账号与 Firebase 项目权限仍在，这份文件足以让另一台电脑继续维护当前游戏。
